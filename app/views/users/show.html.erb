


<div>
  <%= image_tag @user.get_profile_image(100,100) %>
  <p>name</p>
  <p><%= @user.name %></p>
  <p>introduction</p>
  <p><%= @user.introduction %></p>

  <p><%= link_to "User info edit(仮)", edit_user_path(@user.id) %></p>
</div>


<h2>Books</h2>
  <h4>Title</h4>
  <h4>Opinion</h4>
<% @books.each do |book| %>
<div>
  <p><%= link_to book.title, book_path(book.id) %>
  <p><%= book.body %></p>
</div>
<% end %>
