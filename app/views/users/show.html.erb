<div>
  <%= flash[:notice] %>
</div>

<%= render 'books/list.html.erb', user: @user %>


<h2>Books</h2>
  <h4>Title</h4>
  <h4>Opinion</h4>
<% @books.each do |book| %>
<div>
  <p><%= link_to book.title, book_path(book.id) %>
  <p><%= book.body %></p>
</div>
<% end %>
